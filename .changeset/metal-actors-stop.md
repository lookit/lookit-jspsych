---
"@lookit/lookit-initjspsych": minor
---

The `lookit-initjspsych` package now checks for VideoAssent trials and wraps the
on_finish function so that the experiment is aborted if the participant clicks
the no button.
